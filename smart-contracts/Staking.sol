contract Staking {
    mapping(address => uint256) public stakes;
    function stake(uint256 amount) external {
        // Transfer ALSC from user to contract
        // Update staked balance
    }
}